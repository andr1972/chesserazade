/// Placeholder test file for the scaffold commit. The real type tests are
/// added in the commit that introduces the types themselves.
#include <catch2/catch_test_macros.hpp>

TEST_CASE("scaffold sanity", "[scaffold]") {
    REQUIRE(1 + 1 == 2);
}
